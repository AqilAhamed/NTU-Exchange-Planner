# --- Secrets -------------------------------------------------------------
.env
*.env
!.env.example
backend/.env

# --- Databases (coursefinder.db is 187 MB; app_state.db is runtime state) --
*.db
*.db-wal
*.db-shm
!coursefinder.db

# --- Python --------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Node / Next ---------------------------------------------------------
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts.timestamp

# --- Editors / OS --------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log

# Generated by the evaluation run; the numbers live in docs/METRICS.md
backend/metrics_report.json

# --- Submission deliverables ----------------------------------------------
# The judging deck and video script are submitted through the hackathon
# portal, not shipped with the code. Keeping them out also keeps the
# pptxgenjs node_modules and the generated .pptx out of the repository.
deck/
